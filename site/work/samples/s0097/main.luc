func main():
    var n = 1
    var seen = 0
    while true:
        n *= 3
        if n % 2 == 0:
            continue
        seen += 1
        if seen == 5:
            break
    print(f"stopped at {n} after {seen} odd values")
