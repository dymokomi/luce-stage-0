pub class Counter:
    pub value: i64

    pub init(start: i64):
        self.value = start

    pub func next() -> i64:
        self.value += 1
        return self.value

pub func main(args: list[str]):
    let counter = Counter(start = len(args))
    let same = counter
    print(str(same.next()))
