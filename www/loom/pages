# The pages of loom.luciaos.com, in order.
#
# One row per page: SLUG | NAV LABEL | TITLE | DESCRIPTION.  The first
# row is the front page and its slug is empty, so it is written to
# `out/index.html`; every other row becomes `out/SLUG/index.html`.
#
# The order here is the order of the tabs in the bar and the order the
# Previous/Next links walk, so adding a page is one row and a file
# under `content/`.  Nothing else lists the pages.

	Overview	loom	The small host and runner for compiled Luce library artifacts, with no interpreter or code generator inside.
running	Running	Running programs	How loom validates and starts a .lc, compiles and caches a .luc, finds the compiler, and reports the result.
shell	Shell	The shell	loom's interactive line shell — its five commands, bare program paths, colour, and what a piped script gets that a person at a prompt does not.
editor	Editor	The editor	The shipped multi-buffer terminal editor: search, undo, build/run, panes, draft recovery, and termui, all written in Luce.
host	Host	The host	The one versioned boundary for files, terminals, workers, windows, GPU surfaces, clocks, process services, and fail-closed capability.
direction	Direction	Where loom is going	Why the language comes first, the ordered ARC/classes/closures roadmap, and what that means for a deliberately small loom.
