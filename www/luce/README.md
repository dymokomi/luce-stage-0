# www/luce — luce.luciaos.com

The Luce language's documentation: a tour, worked examples, a
reference, and the status page that says out loud what the language
cannot yet do.

It lives in this repository so that it changes with the language, and
it is generated rather than hand-published so that it cannot quietly
stop being true.

## The contract

**Every Luce sample on this site is compiled and run by the freshly
built toolchain, and the output printed under it is the output that
run produced.**  Not an output somebody typed once.

- A fenced ` ```luce ` block must say what is to become of it — `run`,
  `trap`, `raise`, `fail` or `module`.  A bare ` ```luce ` fence is a
  build error naming the page and the line, so there is no unverified
  Luce here by construction.
- `run`, `trap` and `raise` each need an ` ```output ` fence
  immediately after, compared **byte for byte**.  A trap and an
  uncaught error are different exit statuses, and a sample captioned
  "traps" that actually raised fails.
- `fail` is refused by `luce check`, and its output is the diagnostic.
- ` ```console ` runs its `$ ` lines with the built toolchain first on
  `PATH` and rebuilds the transcript from what they really printed.
- Attributes: `file=NAME.luc` names the file written, `args=…` are the
  program's arguments, and `include=PATH` shows a file from the
  repository instead of a fence body — so the pages that document
  `programs/*.luc` change when those programs do, or the build stops.
- Every generated page is then walked for links, and every `href` must
  resolve to a file in the output tree **with the anchor it names**.

`www/luce/src/verify.zig`'s header is the full fence vocabulary.

Separately, `www/luce/src/coverage.zig` holds the reference pages to the
compiler's own lists — every builtin, every receiver method, every
trap and error code, every std function and constant, every
command-line option — read out of `src/` at test time.  A name the
compiler has that the reference does not is a failed build, on the
commit that added it.  Samples prove the code runs; that proves the
prose around it is still true.

## Build

```sh
./www/luce/build.sh          # toolchain, then generator, then pages
./www/luce/build.sh --fast   # trust the toolchain already in build/
```

It builds `build/luce` and `build/loom` first, because the site is
generated by running them; then compiles the generator, runs the
generator's own tests, and writes `www/luce/out`.  A sample that stops
working stops the script.

The generator's tests also run under the repository's `zig build test`
(wired in `build.zig`), so a change to the language that the site has
to follow fails on the commit that made it rather than on whoever next
builds the site.

## Add a page

Write a Markdown file under `content/SECTION/`.  The first heading is
the title; the section and slug come from the path.  Sections and
their order are `www/luce/src/site.zig`.  Every sample you write is
checked, so write the output you *expect* and let the build tell you
what actually happened.

## Deploy

```sh
./www/luce/deploy.sh          # build from clean, then publish
./www/luce/deploy.sh --fast   # publish what is already in www/luce/out
```

It refuses to publish an unbuilt tree, then hands `www/luce/out` to
`www/deploy/publish.sh`, which mirrors it to `/opt/apps/luce_docs` on
the edge server and curls the live URL afterwards.  `LUCE_SITE_ROOT`
overrides the static root; the host and the key are shared with the
other two sites and are `LUCIAOS_EDGE_HOST` and `LUCIAOS_EDGE_KEY`.

**The default target is a single host**, named in
`www/deploy/publish.sh` and nowhere else — see `www/README.md`.  There
is no infrastructure-as-code for it, no second host, and no record in
this repository of the Caddy configuration; moving the site means
editing that one line and knowing where the certificate comes from.

## Layout

```text
www/luce/
  build.sh      toolchain -> generator -> pages
  deploy.sh     publish www/luce/out
  content/      the Markdown, one directory per section
  assets/       CSS and anything else copied verbatim
  src/          the generator
    main.zig        walk content, render, check links
    site.zig        sections, slugs, ordering
    markdown.zig    the Markdown subset, with a fence sink
    verify.zig      compile and run every sample; the fence vocabulary
    coverage.zig    hold the reference to the compiler's own lists
    highlight.zig   Luce syntax highlighting
    page.zig        the HTML shell
    buffer.zig      an output buffer that escapes
  out/          generated; not committed
  work/         the generator binary and per-sample scratch; not committed
```
