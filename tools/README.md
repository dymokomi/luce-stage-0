# tools

Developer tooling that ships with the repo but isn't part of loom.

- `vscode-luce/` — syntax highlighting for `.luc`.

Everything else here tested the hand-written code generators and went
away with them.
