# A page whose sample is stale

Prose may say Int as often as it likes — this sentence is about a name
the language used to have, and the guard must not read prose.

```zig
const held: Float = 1.5;
```

A fence of another language is not a Luce sample either.

```luce
func main():
    var xs: List(Int) = []
    assert(len(xs) == 0)
```

```output
```
