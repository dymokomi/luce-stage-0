# A dirty tree, on purpose

`docs/BROKEN.md` is a deliberately inconsistent miniature document. The
documentation checker points at it to prove that its clean-tree assertion is
actually capable of failing.

**Nothing here ships.** It is repository-tool input only.
