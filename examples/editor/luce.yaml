# The editor is a dependency-carrying program (docs/PACKAGES.md): it
# draws through termui, resolved from whichever shelf LUCE_LIB names.
#
#   LUCE_LIB=$PWD/build/lib:$PWD/packages luce build examples/editor/editor.luc
name: editor
version: 2.0.0

packages:
  termui: 0.4.0
