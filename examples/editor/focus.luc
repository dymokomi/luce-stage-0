# Which pane the keyboard is talking to.
#
# The application uses this one value to style the active view, route keys and
# decide which cursor to expose. Search is a mode over the status line rather
# than a separate region.
enum Focus:
    editor
    files
    output
    search
