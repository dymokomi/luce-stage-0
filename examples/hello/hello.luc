# The smallest Luce program.
#
#   luce build examples/hello/hello.luc
#   ./examples/hello/hello you

pub func main(args: list[str]):
    var name = "loom"
    if len(args) > 0:
        name = args[0]
    print("hello, " + name)
